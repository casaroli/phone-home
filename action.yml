name: 'Phone home to report status'
description: 'Reports the workflow status to the dispatcher'
inputs:
  phone-home-input:
    description: Status report data from dispatch-phone-home
    required: false

runs:
  using: 'node20'
  main: 'index.js'
